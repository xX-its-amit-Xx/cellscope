# SPDX-License-Identifier: GPL-3.0-or-later
# CellScope backend runtime dependencies.
# Version floors are chosen for compatibility with Python 3.11 and pydantic v2.
# Upper bounds are intentionally loose; CI resolves and tests the exact pins.

# --- Web framework / ASGI ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9          # multipart/form-data upload parsing

# --- Configuration / validation ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- Single-cell / numerics ---
anndata>=0.10,<0.12
scanpy>=1.10,<1.11
numpy>=1.26,<2.0                 # scanpy 1.10 stack is most stable on numpy 1.x
scipy>=1.11,<2.0
pandas>=2.1,<3.0
h5py>=3.10,<4.0                  # backed-mode .h5ad I/O

# --- Clustering / embedding ---
leidenalg>=0.10,<1.0             # leiden community detection
igraph>=0.11,<1.0                # python-igraph; graph backend for leiden
umap-learn>=0.5.5,<1.0
scikit-learn>=1.4,<2.0           # PCA / neighbors fallbacks
